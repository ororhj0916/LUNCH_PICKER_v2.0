
  # Lunch Picker App

  This is a code bundle for Lunch Picker App. The original project is available at https://www.figma.com/design/sPpaAxlxSzQ8upJ0FAeFGq/Lunch-Picker-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  